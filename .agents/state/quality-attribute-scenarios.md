# Quality Attribute Scenarios

Last updated: 2026-05-14

| ID | Attribute | Scenario | Measure | Verification | Linked Requirements | Status | Last Updated |
| --- | --- | --- | --- | --- | --- | --- | --- |
| QA-000 | process | Agents verify significant claims before reporting completion. | Completed missions link evidence to requirement IDs. | inspection | REQ-FUNC-000 | verified | 2026-05-11 |
| QA-CCOS-001 | reliability | A Company OS lifecycle trace is executed locally in Docker after migrations and seed. | One command proves approval, stage lifecycle, automation evaluator, event readback, and audit readback. | V1EVID-001 Docker smoke | REQ-CCOS-001; REQ-CCOS-002; REQ-CCOS-003; REQ-CCOS-004 | verified | 2026-05-11 |
| QA-CCOS-002 | security | Risky Company OS lifecycle writes remain capability and command bounded. | Smoke uses HTTP command routes and verifies event/audit side effects; no raw status patching path is introduced. | source review plus V1EVID-001 Docker smoke | REQ-CCOS-001; REQ-CCOS-003 | verified | 2026-05-11 |
| QA-OM-001 | reliability | Operating model registry lifecycle APIs are exercised locally in Docker after migrations and seed. | One command proves folder, storage location, knowledge root, and automation definition create/read/update/delete plus aggregate readback. | V1EVID-002 Docker smoke | REQ-OM-001; REQ-OM-002 | verified | 2026-05-11 |
| QA-OM-002 | security | Operating model registry writes remain workspace-scoped. | Foreign workspace scope use and foreign update attempts return `404`. | V1EVID-002 Docker smoke | REQ-OM-003 | verified | 2026-05-11 |
| QA-V2-001 | maintainability | V2 work starts from one selected lane and one scoped next task. | Active queues point to V2AGENT-001, and delivery map records Agent-First Company OS as selected while external blockers remain separate. | V2PLAN-001 source-of-truth inspection | REQ-V2-001 | verified | 2026-05-11 |
| QA-MCP-001 | security | Read-only MCP profiles expose only read tools. | `mcp_company_os_reader` manifest does not include approval request/decision, stage lifecycle, or automation execution tools; direct write calls return `403`. | V2AGENT-002 Docker API regression test | REQ-MCP-001 | verified | 2026-05-11 |
| QA-MCP-002 | security | MCP bridge blocks risky tools unless supervised mode is explicit. | Default `tools/call` for manifest `requiresApproval` tools returns a structured blocked result; safe read tools still call the HTTP API. | V2AGENT-004 Docker API bridge regression test | REQ-MCP-003 | verified | 2026-05-11 |
| QA-MCP-003 | security | Supervised MCP mode reaches API policy without bypassing it. | A controlled risky tool call with `COMPANYCORE_MCP_COMMAND_MODE=supervised_operator` reaches HTTP validation and returns the expected API rejection on completed disposable data. | V2AGENT-005 Docker API bridge regression test | REQ-MCP-004 | verified | 2026-05-11 |
| QA-CCOS-003 | usability/security | Owner can scan agent command pressure before allowing risky work. | Company OS cockpit renders queue markers for approvals, blocked execution, automation dry-run guidance, and owner-gated next actions. | V2AGENT-006R system Chrome dump-DOM render proof | REQ-CCOS-005 | verified | 2026-05-14 |
| QA-WEB-AGENT-001 | usability/security | Owner can inspect the same MCP tool surface an agent receives before creating or using agent credentials. | Web groups tools by capability, route family, risk level, and `requiresApproval`, and links risky Company OS tools to supervised command context. | V2WEB-AGENT-001 `npm run build` plus Browser plugin render and family-filter proof | REQ-WEB-AGENT-001 | verified | 2026-05-14 |
| QA-WEB-AGENT-002 | usability/reliability | Owner can read one Company OS action chain without switching between raw event and audit tables. | Correlation timeline renders related event and audit records in timestamp order with actor, resource, kind, and payload preview. | V2WEB-AGENT-002 `npm run build` plus Playwright fallback render proof | REQ-WEB-AGENT-002 | verified | 2026-05-14 |
| QA-WEB-AGENT-003 | usability/maintainability | Owner can inspect how operating structure, provider paths, resources, guardrails, automation, and runtime evidence relate before using command panels. | Operating graph detail renders selected area tables plus linked Company OS graph nodes from existing read contracts. | V2WEB-AGENT-003 `npm run build` plus Playwright render proof | REQ-WEB-AGENT-003 | verified | 2026-05-14 |
| QA-WEB-AGENT-004 | usability/security | Owner can understand the likely effect and recovery path before running approval, stage, or automation commands. | Command panels render readiness, expected result, recovery guidance, and automation proposal/evidence output while writes still use existing audited routes. | V2WEB-AGENT-004 `npm run build` plus Playwright render/interaction proof | REQ-WEB-AGENT-004 | verified | 2026-05-14 |
| QA-WEB-AGENT-005 | security/maintainability | Definition editors cannot be added as raw CRUD without an approved write contract. | Architecture contract names allowed write classes and blocks raw runtime evidence editing. | V2WEB-AGENT-005 source review plus architecture doc | REQ-WEB-AGENT-005 | verified | 2026-05-14 |
| QA-WEB-AGENT-006 | security/reliability | A low-risk definition write cannot bypass workspace scope, capability scope, or audit/event evidence. | Owner write paths succeed; foreign workspace and read-only service paths fail closed; MCP manifest marks archive as destructive. | V2WEB-AGENT-006 `npm test` against disposable PostgreSQL | REQ-WEB-AGENT-006 | verified | 2026-05-14 |
| QA-WEB-AGENT-007 | usability/security | Owner can edit only low-risk standards from the Company OS cockpit, with capability denial visible for sessions that cannot write definitions. | Standards editor renders with local form/table states and uses only audited standards write routes; higher-risk Company OS objects stay outside raw editing. | V2WEB-AGENT-007 `npm run build`, static marker checks, `git diff --check`, and V2WEB-AGENT-007R Playwright Chromium render/create proof | REQ-WEB-AGENT-007 | verified | 2026-05-14 |
| QA-WEB-AGENT-008 | security/maintainability | Workflow definition editors cannot mutate active agent behavior through table CRUD. | Architecture requires draft command routes, impact preview, approval/activation evidence, versioning, and MCP supervision before any workflow editor ships. | V2WEB-AGENT-008 source review plus architecture contract | REQ-WEB-AGENT-008 | verified | 2026-05-14 |
| QA-WEB-AGENT-009 | security/reliability | Workflow definition draft commands fail closed for read-only keys, cross-workspace roots, and repeated idempotent submissions while preserving audit/event evidence. | Owner paths succeed; idempotency returns the original draft; foreign workspace and read-only paths fail closed; impact preview reports affected counts and approval reasons. | V2WEB-AGENT-009 `npm test` against disposable PostgreSQL | REQ-WEB-AGENT-009 | verified | 2026-05-14 |
| QA-WEB-AGENT-010 | security/reliability | Workflow definition activation creates a new version only for root types with tested versioning and blocks broader roots until schema support exists. | Procedure activation requires approved evidence when preview requires it, deprecates the old version, creates a new active version, emits audit/event evidence, and blocks process/pipeline activation. | V2WEB-AGENT-010 `npm test` against disposable PostgreSQL | REQ-WEB-AGENT-010 | verified | 2026-05-14 |
| QA-WEB-AGENT-011 | security/data integrity | Process and pipeline activation uses real version uniqueness instead of overwriting active workflow roots. | Migration changes process/pipeline unique keys to include `version`; activation creates new active root rows and deprecates previous roots while preserving runtime links to old IDs. | V2WEB-AGENT-011 `npm test` against disposable PostgreSQL | REQ-WEB-AGENT-011 | verified | 2026-05-14 |
| QA-WEB-AGENT-012 | usability/security | Owner can operate workflow definition changes from the web without raw-editing active workflow roots. | Cockpit creates a draft, previews impact, shows approval reasons, and keeps activation gated by capability and approved approval evidence. | V2WEB-AGENT-012 `npm run build` plus real-backend Playwright create/preview proof | REQ-WEB-AGENT-012 | verified | 2026-05-14 |
| QA-WEB-AGENT-014 | usability/security | Authorized owners can resume interrupted workflow draft work without exposing draft payloads to read-only sessions. | Draft list/detail routes are workspace-scoped and capability-gated; the cockpit lists only open drafts for the selected root type and can resume a selected draft into the command panel. | V2WEB-AGENT-014 `npm test` against disposable PostgreSQL on `localhost:55438` plus real-backend Playwright draft-only history/resume proof on `http://127.0.0.1:3102/react-company-os` | REQ-WEB-AGENT-014 | verified | 2026-05-14 |
| QA-WEB-AGENT-016 | security/reliability | Workflow recovery commands cannot hide active behavior or bypass audit evidence. | Archive blocks active root versions and inactive roots with active runtime dependencies; safe inactive archive emits event/audit evidence and is idempotent. | V2WEB-AGENT-016 `npm test` against disposable PostgreSQL on `localhost:55439` | REQ-WEB-AGENT-016 | verified | 2026-05-14 |
| QA-WEB-AGENT-017 | security/reliability | Workflow rollback cannot bypass preview and activation controls. | Rollback creates a normal draft from a historical source version, records rollback source metadata, generates impact preview, and leaves activation as a separate command. | V2WEB-AGENT-017 `npm test` against disposable PostgreSQL on `localhost:55440` | REQ-WEB-AGENT-017 | verified | 2026-05-14 |
| QA-WEB-AGENT-019 | usability/security | Owner can start workflow recovery from the cockpit without bypassing backend command gates. | Recovery controls show inactive historical versions, require a reason, separate archive from rollback draft, and load rollback drafts into the existing draft panel. | V2WEB-AGENT-019 `npm run build`, `npm test` against disposable PostgreSQL on `localhost:55441`, and Playwright proof on `http://127.0.0.1:3103/react-company-os` | REQ-WEB-AGENT-019 | verified | 2026-05-14 |
| QA-WEB-AGENT-021 | reliability/data integrity | Workflow recovery still works when a later active version has a different visible name than the historical rollback source. | Process, pipeline, and procedure roots preserve `family_id`; rollback-draft active target lookup uses family lineage instead of mutable name. | V2WEB-AGENT-021 `npm test` against disposable PostgreSQL on `localhost:55442` | REQ-WEB-AGENT-021 | verified | 2026-05-14 |
| QA-WEB-AGENT-022 | reliability/UX | Company OS cockpit does not create noisy 404s while loading operating graph and table previews. | Company OS collection slugs are requested through `/v1/company-os/:collection`, not generic `/v1/:slug`. | V2WEB-AGENT-022 `npm run build` plus Playwright mock render request proof | REQ-WEB-AGENT-022 | verified | 2026-05-14 |
| QA-WEB-AGENT-023 | usability/security | Owner can finish workflow recovery from the command panel without switching to API-only steps. | Recovery controls preserve local draft state through rollback draft creation, preview, approval request, approval decision, and activation; every write still calls audited backend command routes. | V2WEB-AGENT-023 `npm run build` plus Playwright interaction proof | REQ-WEB-AGENT-023 | verified | 2026-05-14 |
| QA-WEB-AGENT-024 | reliability | The workflow recovery UI proof works against a migrated backend and seeded PostgreSQL, not only a mocked API. | Disposable backend proof runs the owner cockpit against real HTTP routes and cleans up Compose resources afterward. | V2WEB-AGENT-024 Docker Compose real-backend Playwright proof | REQ-WEB-AGENT-024 | verified | 2026-05-14 |
| QA-AGRUN-007 | security/data integrity | Google Drive import must ingest only explicit department roots and must not assign subfolders to other departments. | Production import selected only numbered roots `00`-`12`; each root was mapped to its operating area; descendant scope verification returned `mismatches=[]` and `unassignedCount=0`. | AGRUN-007 production import and scope readback: 748 items, 171 folders, 13 mappings. | REQ-AGRUN-007 | verified | 2026-05-14 |

Scenario shape:

`stimulus -> environment -> artifact -> response -> measure -> verification`
